import XCTest
@testable import ImprovedAnimations

final class ImprovedAnimationsTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(ImprovedAnimations().text, "Hello, World!")
    }
}
